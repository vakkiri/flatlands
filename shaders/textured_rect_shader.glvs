#version 140

uniform mat4 pcMatrix;

in vec2 vertexPos;
in vec2 itexCoord;
out vec2 texCoord;

void main() {
	texCoord = itexCoord;
	gl_Position = pcMatrix * vec4( vertexPos.x, vertexPos.y, 0.0, 1.0 );
}

