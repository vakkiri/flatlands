#version 420
uniform sampler2D texUnit;

in vec2 texCoord;
layout(location = 0) out vec4 gl_FragColor;

void main() {
	vec2 flippedCoord = vec2( texCoord.x, 1 - texCoord.y );
	gl_FragColor = texture( texUnit, flippedCoord );
	gl_FragColor.r = 1.f;
	gl_FragColor.a = 0.5;
}


